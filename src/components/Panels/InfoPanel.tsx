import './Panels.css';

type Props = {
    totalDistance: number,
    totalTime: Date,
}

export default function InfoPanel({ totalDistance, totalTime }: Props) {
    const strDistance: string = totalDistance + " KM";
    let totalTime: string;

    if (totalTime.)

    return (
        <div className={"info-panel"}>
            <h1>Total</h1>
            <div className={"total-info"}>
                <h2>{}</h2>
            </div>
        </div>
    )
}